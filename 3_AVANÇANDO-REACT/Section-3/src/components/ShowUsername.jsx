const ShowUsername = ({ nome, idade }) => {
  return (
    <div>
      <h2>O nome do usuário é: {nome}</h2>
      <h2>A idade do usuário é: {idade}</h2>
      
    </div>
  );
};

export default ShowUsername;
